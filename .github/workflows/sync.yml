name: Sync Leaderboard Data
# v2

on:
  schedule:
    - cron: '0 */1 * * *'   # every hour
  workflow_dispatch:          # manual trigger

concurrency:
  group: sync
  cancel-in-progress: false   # let a running sync finish, queue the next

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Pull latest
        run: git pull

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Fetch leaderboard data
        env:
          API_TOKEN: ${{ secrets.CIRCLE21_TOKEN }}
          SITE_URL: ${{ vars.SITE_URL }}
        run: python3 fetch.py

      - name: F/F virtual team simulation (Anna + Corinna)
        run: python3 simulate_ff.py

      - name: Commit & push data.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ feeds.html
          if git diff --cached --quiet; then
            echo "No changes — nothing to commit"
          else
            git commit -m "chore: sync leaderboard data [skip ci]"
            git push
          fi
